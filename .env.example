# MDNotebook is local-first and does not require GitHub or OAuth variables.
# Keep this file for future optional configuration.

# Optional: override the notes storage root path.
# By default web mode uses ./content/docs.
# Desktop mode sets this automatically to the OS user-data folder.
# MDNOTEBOOK_NOTES_ROOT=/absolute/path/to/content/docs
